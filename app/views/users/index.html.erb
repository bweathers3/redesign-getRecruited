<h1>All Users</h1>
<% @users.each do |user| %>
  <div class="media">
    <div class="media-body">
      <h4 class="media-heading">
        <p><%= user.firstName + " " + user.secondName + "             " %><%= link_to user.email, user_path(user.id) %></p>
      </h4>
    </div>
  </div>
<% end %>
